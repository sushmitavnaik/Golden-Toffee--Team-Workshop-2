# Student-Generated Input Assets

Save the unchanged chart produced in ordinary ChatGPT here as `baseline_chart.png`.

Do not overwrite it during improvement. The specialist agent writes its improved chart to `responses/improved_chart.png` so the two versions remain available for comparison.

